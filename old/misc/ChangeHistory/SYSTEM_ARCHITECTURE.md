# プログラマー適性検査システム - 技術構成図

**作成日**: 2025年12月19日  
**バージョン**: 1.0.0

---

## 📊 システム全体構成図

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Windows 11 PC                                  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         WSL2 (Ubuntu)                                 │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │                    Docker Desktop                               │  │  │
│  │  │                                                                 │  │  │
│  │  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │  │  │
│  │  │   │   Laravel   │  │    MySQL    │  │    Redis    │            │  │  │
│  │  │   │    :80      │  │    :3306    │  │    :6379    │            │  │  │
│  │  │   │  (PHP 8.4)  │  │    (8.0)    │  │   (alpine)  │            │  │  │
│  │  │   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │  │  │
│  │  │          │                │                │                    │  │  │
│  │  │          └────────────────┴────────────────┘                    │  │  │
│  │  │                           │                                     │  │  │
│  │  │   ┌─────────────┐  ┌──────┴──────┐  ┌─────────────┐            │  │  │
│  │  │   │  Selenium   │  │  Meilisearch │  │   Mailpit   │            │  │  │
│  │  │   │  (Chrome)   │  │    :8800     │  │ :1025/:8025 │            │  │  │
│  │  │   └─────────────┘  └─────────────┘  └─────────────┘            │  │  │
│  │  │                                                                 │  │  │
│  │  │   ┌─────────────┐                                              │  │  │
│  │  │   │ phpMyAdmin  │                                              │  │  │
│  │  │   │    :8080    │                                              │  │  │
│  │  │   └─────────────┘                                              │  │  │
│  │  │                                                                 │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────┐                                                      │
│  │    ブラウザ        │  ← ユーザーアクセス (http://localhost:80)            │
│  │  (Chrome/Edge)    │                                                      │
│  └───────────────────┘                                                      │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🐳 Dockerサービス構成

| サービス名   | イメージ                     | ポート     | 用途                     |
| ------------ | ---------------------------- | ---------- | ------------------------ |
| laravel.test | sail-8.4/app                 | 80, 5173   | アプリケーションサーバー |
| mysql        | mysql/mysql-server:8.0       | 3306       | データベース             |
| redis        | redis:alpine                 | 6379       | セッション・キャッシュ   |
| meilisearch  | getmeili/meilisearch         | 8800       | 全文検索エンジン         |
| mailpit      | axllent/mailpit              | 1025, 8025 | メール送信テスト         |
| selenium     | selenium/standalone-chromium | -          | E2Eテスト用ブラウザ      |
| phpmyadmin   | phpmyadmin/phpmyadmin        | 8080       | DB管理ツール             |

---

## 🔧 バックエンド技術スタック

```
┌─────────────────────────────────────────────────────────────┐
│                    バックエンド (PHP)                        │
├─────────────────────────────────────────────────────────────┤
│  Laravel Framework 12.x                                     │
│  ├── PHP 8.4                                                │
│  ├── Laravel Sail (Docker環境)                              │
│  ├── Laravel Sanctum (API認証・CSRF)                        │
│  ├── Laravel Breeze (認証スカフォールド)                     │
│  ├── Inertia.js (サーバーサイド)                            │
│  └── Ziggy (ルート生成)                                     │
├─────────────────────────────────────────────────────────────┤
│  追加パッケージ                                              │
│  ├── barryvdh/laravel-dompdf (PDF生成)                      │
│  ├── barryvdh/laravel-debugbar (デバッグ)                   │
│  └── laravel/pail (ログビューア)                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 フロントエンド技術スタック

```
┌─────────────────────────────────────────────────────────────┐
│                 フロントエンド (TypeScript)                  │
├─────────────────────────────────────────────────────────────┤
│  Vue.js 3.5.x                                               │
│  ├── TypeScript 5.9                                         │
│  ├── Inertia.js (クライアントサイド)                        │
│  ├── Axios (HTTP通信)                                       │
│  └── Vue Router (SPA遷移)                                   │
├─────────────────────────────────────────────────────────────┤
│  スタイリング                                                │
│  ├── Tailwind CSS 3.x                                       │
│  ├── @tailwindcss/forms                                     │
│  ├── PostCSS                                                │
│  └── Autoprefixer                                           │
├─────────────────────────────────────────────────────────────┤
│  ビルドツール                                                │
│  ├── Vite 6.x                                               │
│  ├── laravel-vite-plugin                                    │
│  ├── @vitejs/plugin-vue                                     │
│  └── Terser (圧縮)                                          │
├─────────────────────────────────────────────────────────────┤
│  コード品質                                                  │
│  ├── ESLint 9.x                                             │
│  ├── Prettier                                               │
│  ├── vue-tsc (型チェック)                                   │
│  └── @typescript-eslint                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 データフロー図

```
┌──────────────────────────────────────────────────────────────────────────┐
│                          リクエスト/レスポンス フロー                      │
└──────────────────────────────────────────────────────────────────────────┘

  ブラウザ                Nginx/PHP-FPM              Laravel
     │                        │                        │
     │  HTTP Request          │                        │
     │───────────────────────>│                        │
     │                        │  PHP処理               │
     │                        │───────────────────────>│
     │                        │                        │
     │                        │        ┌───────────────┼───────────────┐
     │                        │        │               │               │
     │                        │        ▼               ▼               ▼
     │                        │   ┌────────┐     ┌────────┐     ┌────────┐
     │                        │   │ MySQL  │     │ Redis  │     │Session │
     │                        │   │  Data  │     │ Cache  │     │ Store  │
     │                        │   └────────┘     └────────┘     └────────┘
     │                        │        │               │               │
     │                        │        └───────────────┼───────────────┘
     │                        │                        │
     │                        │  Inertia Response      │
     │                        │<───────────────────────│
     │  HTML + Vue Props      │                        │
     │<───────────────────────│                        │
     │                        │                        │
     ▼                        │                        │
  Vue.js                      │                        │
  クライアント                 │                        │
     │                        │                        │
     │  AJAX (Axios)          │                        │
     │───────────────────────>│───────────────────────>│
     │                        │                        │
     │  JSON Response         │                        │
     │<───────────────────────│<───────────────────────│
     │                        │                        │
```

---

## 🧪 テストツール

```
┌─────────────────────────────────────────────────────────────┐
│                      テスト環境                              │
├─────────────────────────────────────────────────────────────┤
│  E2Eテスト                                                  │
│  ├── Playwright 1.57                                        │
│  ├── Selenium (standalone-chromium)                         │
│  └── テストレポート (port 8888)                             │
├─────────────────────────────────────────────────────────────┤
│  ユニットテスト                                              │
│  ├── PHPUnit 11.x                                           │
│  ├── Mockery                                                │
│  └── Laravel Testing                                        │
├─────────────────────────────────────────────────────────────┤
│  静的解析                                                    │
│  ├── Laravel Pint (PHP CS)                                  │
│  ├── ESLint (TypeScript/Vue)                                │
│  └── vue-tsc (型チェック)                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 ディレクトリ構造

```
ProgrammerAptitudeTest/
├── app/                          # Laravelアプリケーション
│   ├── Console/                  # Artisanコマンド
│   ├── Http/
│   │   ├── Controllers/          # コントローラー
│   │   └── Middleware/           # ミドルウェア
│   ├── Models/                   # Eloquentモデル
│   └── Providers/                # サービスプロバイダ
├── config/                       # 設定ファイル
├── database/
│   ├── factories/                # モデルファクトリ
│   ├── migrations/               # マイグレーション
│   └── seeders/                  # シーダー
├── resources/
│   ├── js/
│   │   ├── Components/           # Vue共通コンポーネント
│   │   ├── Layouts/              # レイアウト
│   │   └── Pages/                # ページコンポーネント
│   │       ├── Admin/            # 管理者画面
│   │       ├── Auth/             # 認証画面
│   │       └── Practice/         # 練習問題
│   ├── css/                      # スタイルシート
│   └── views/                    # Bladeテンプレート
├── routes/
│   ├── web.php                   # Webルート
│   └── api.php                   # APIルート
├── e2e/                          # Playwrightテスト
├── tests/                        # PHPUnitテスト
├── public/                       # 公開ディレクトリ
├── storage/                      # ストレージ
├── docker-compose.yml            # Docker構成
├── vite.config.js                # Vite設定
└── playwright.config.ts          # Playwright設定
```

---

## 🔐 認証・セキュリティフロー

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CSRF トークン フロー                              │
└─────────────────────────────────────────────────────────────────────────┘

  1. 初回アクセス
     ┌─────────┐     GET /sanctum/csrf-cookie     ┌─────────┐
     │ Browser │ ──────────────────────────────> │ Laravel │
     │         │ <────────────────────────────── │         │
     └─────────┘     Set-Cookie: XSRF-TOKEN      └─────────┘

  2. ページ読み込み
     ┌─────────┐     GET /page                    ┌─────────┐
     │ Browser │ ──────────────────────────────> │ Laravel │
     │         │ <────────────────────────────── │         │
     └─────────┘     Inertia Props:              └─────────┘
                     { csrf_token: "xxx..." }

  3. フォーム送信
     ┌─────────┐     POST /action                 ┌─────────┐
     │ Browser │ ──────────────────────────────> │ Laravel │
     │         │     Cookie: XSRF-TOKEN=xxx      │         │
     │         │     Body: { _token: "xxx" }     │         │
     │         │                                 │         │
     │         │ <────────────────────────────── │         │
     └─────────┘     200 OK / Inertia Response   └─────────┘

                     VerifyCsrfToken
                     ミドルウェアで検証
```

---

## 📊 技術バージョン一覧

| カテゴリ           | 技術                       | バージョン |
| ------------------ | -------------------------- | ---------- |
| **OS**             | Windows 11 + WSL2 (Ubuntu) | -          |
| **コンテナ**       | Docker Desktop             | -          |
| **言語**           | PHP                        | 8.4        |
| **言語**           | TypeScript                 | 5.9        |
| **フレームワーク** | Laravel                    | 12.x       |
| **フレームワーク** | Vue.js                     | 3.5.x      |
| **フレームワーク** | Inertia.js                 | 2.x        |
| **データベース**   | MySQL                      | 8.0        |
| **キャッシュ**     | Redis                      | alpine     |
| **ビルド**         | Vite                       | 6.x        |
| **CSS**            | Tailwind CSS               | 3.x        |
| **テスト**         | Playwright                 | 1.57       |
| **テスト**         | PHPUnit                    | 11.x       |

---

## 🌐 ポート一覧

| ポート | サービス    | 用途                   |
| ------ | ----------- | ---------------------- |
| 80     | Laravel     | メインアプリケーション |
| 3306   | MySQL       | データベース接続       |
| 5173   | Vite        | 開発サーバー (HMR)     |
| 6379   | Redis       | セッション・キャッシュ |
| 8025   | Mailpit     | メールUI               |
| 8080   | phpMyAdmin  | DB管理                 |
| 8800   | Meilisearch | 検索エンジン           |
| 8888   | Playwright  | テストレポート         |

---

**作成者**: GitHub Copilot

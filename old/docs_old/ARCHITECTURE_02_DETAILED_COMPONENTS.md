# システム構成図案 - 2. 詳細アーキテクチャ (コンポーネント図)

## 🔧 フロントエンド詳細

```
┌────────────────────────────────────────────────────────────┐
│                   フロントエンド                           │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │          Vue 3 アプリケーション                       │ │
│  │                                                      │ │
│  │  Pages (Inertiaコンポーネント)                       │ │
│  │  ├─ Welcome.vue (セッションコード入力)            │ │
│  │  ├─ GuestInfo.vue (ゲスト情報入力)               │ │
│  │  ├─ Login.vue (ユーザーログイン)                 │ │
│  │  ├─ Register.vue (新規登録)                      │ │
│  │  ├─ TestStart.vue (試験開始画面)                │ │
│  │  ├─ Part.vue (試験実施)                         │ │
│  │  ├─ Result.vue (結果表示)                       │ │
│  │  ├─ PracticeExplanation.vue (練習問題)          │ │
│  │  ├─ AdminDashboard.vue (管理画面)               │ │
│  │  ├─ AdminEvents.vue (イベント管理)              │ │
│  │  └─ AdminResults.vue (成績管理)                 │ │
│  │                                                      │ │
│  │  Components (再利用可能な部品)                      │ │
│  │  ├─ QuestionCard (問題表示)                      │ │
│  │  ├─ ChoiceButton (選択肢)                        │ │
│  │  ├─ Timer (タイマー)                             │ │
│  │  ├─ ProgressBar (進捗バー)                       │ │
│  │  ├─ Header.vue                                  │ │
│  │  ├─ Navigation.vue                              │ │
│  │  ├─ UserProfile.vue                             │ │
│  │  └─ Modal.vue                                   │ │
│  │                                                      │ │
│  │  Stores (状態管理 - Pinia など)                   │ │
│  │  ├─ authStore (ユーザー情報)                    │ │
│  │  ├─ examStore (試験状態)                        │ │
│  │  └─ uiStore (UI状態)                            │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▲                                │
│                           │ Props/Emit                     │
│                           │                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │          Inertia.js (連携層)                         │ │
│  │                                                      │ │
│  │  ・サーバーから Props 受け取り                      │ │
│  │  ・Vue Component に割り当て                        │ │
│  │  ・フォーム送信時にサーバーと通信                  │ │
│  │  ・ページ遷移を管理                               │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▲                                │
│                           │ Ajax/HTTP                      │
│                           │                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │          Tailwind CSS + CSS Modules               │ │
│  │                                                      │ │
│  │  ・レスポンシブデザイン                            │ │
│  │  ・ダークモード対応                                │ │
│  │  ・アニメーション                                  │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
                            ▲
                    HTTPS通信 (暗号化)
                            │
```

---

## 🔧 バックエンド詳細

```
┌────────────────────────────────────────────────────────────┐
│                   バックエンド (Laravel)                   │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │         Routing (web.php / auth.php)               │ │
│  │                                                      │ │
│  │  GET  /                 → Welcome                  │ │
│  │  POST /session-code/verify                         │ │
│  │  GET  /welcome          → Guest/User welcome       │ │
│  │  POST /login            → Authenticate            │ │
│  │  GET  /test-start                                 │ │
│  │  POST /exam/answer      → 解答送信                │ │
│  │  GET  /result           → 成績表示                │ │
│  │  GET  /logout           → ログアウト              │ │
│  │  GET  /admin/*          → 管理者ページ            │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │          Middleware (ミドルウェア)                  │ │
│  │                                                      │ │
│  │  ├─ VerifyCsrfToken (CSRF 保護)                  │ │
│  │  ├─ Authenticate (認証確認)                      │ │
│  │  ├─ AdminMiddleware (管理者権限)                 │ │
│  │  ├─ CheckSessionCode (セッションコード確認)      │ │
│  │  ├─ HandleInertiaRequests (Props 共有)          │ │
│  │  └─ TrustProxies                                 │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │       Controllers (ビジネスロジック)                │ │
│  │                                                      │ │
│  │  Web:                                              │ │
│  │  ├─ AuthController (ログイン・登録)               │ │
│  │  ├─ ExamController (試験実施)                    │ │
│  │  ├─ SessionCodeController (コード検証)           │ │
│  │  ├─ GuestExamController (ゲスト試験)            │ │
│  │  └─ PracticeController (練習問題)               │ │
│  │                                                      │ │
│  │  Admin:                                            │ │
│  │  ├─ AdminAuthController (管理者認証)             │ │
│  │  ├─ AdminEventController (イベント管理)         │ │
│  │  ├─ AdminResultsController (成績管理)           │ │
│  │  └─ AdminViolationController (不正管理)         │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Services (ビジネスロジック・処理)                 │ │
│  │                                                      │ │
│  │  ├─ GradingService (採点)                        │ │
│  │  ├─ ViolationDetector (不正検知)                │ │
│  │  ├─ SessionManager (セッション管理)             │ │
│  │  ├─ EmailService (メール送信)                  │ │
│  │  └─ ReportGenerator (レポート生成)            │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Models (Eloquent ORM)                             │ │
│  │                                                      │ │
│  │  ├─ User (ユーザー)                              │ │
│  │  ├─ Admin (管理者)                               │ │
│  │  ├─ Event (イベント)                             │ │
│  │  ├─ ExamSession (試験セッション)                │ │
│  │  ├─ Question (問題)                              │ │
│  │  ├─ Choice (選択肢)                              │ │
│  │  ├─ Answer (解答)                                │ │
│  │  ├─ ExamViolation (不正)                         │ │
│  │  └─ PracticeQuestion (練習問題)                 │ │
│  │                                                      │ │
│  │  各 Model に以下を実装:                           │ │
│  │  ・リレーション (HasMany, BelongsTo等)          │ │
│  │  ・キャスト (attributes cast)                   │ │
│  │  ・スコープ (query scope)                       │ │
│  │  ・メソッド (カスタム処理)                      │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Cache / Session                                   │ │
│  │                                                      │ │
│  │  ├─ Redis (キャッシング・セッション)             │ │
│  │  ├─ Database Cache                              │ │
│  │  └─ Cookie (CSRF・Auth)                         │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
                            ▼
                      SQL Query
                            │
```

---

## 🗄️ データベース詳細アーキテクチャ

```
┌────────────────────────────────────────────────────────┐
│                  MySQL Database                        │
├────────────────────────────────────────────────────────┤
│                                                        │
│  【認証層 - Authentication Layer】                    │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ├─ users (一般ユーザー)                         │ │
│  │ │  ├─ id, name, email, password                │ │
│  │ │  ├─ admission_year, email_verified_at        │ │
│  │ │  └─ created_at, updated_at                   │ │
│  │ │                                               │ │
│  │ └─ admins (管理者)                              │ │
│  │    ├─ id, name, email, password                │ │
│  │    └─ created_at, updated_at                   │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  【試験管理層 - Exam Management Layer】               │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ├─ events (試験イベント)                        │ │
│  │ │  ├─ id, name, passphrase                     │ │
│  │ │  ├─ begin_at, end_at, time_limit            │ │
│  │ │  ├─ part1_questions, part2_questions, ...   │ │
│  │ │  └─ created_at, updated_at                  │ │
│  │ │                                               │ │
│  │ ├─ exam_sessions (ユーザーセッション)         │ │
│  │ │  ├─ id, user_id (FK), event_id (FK)         │ │
│  │ │  ├─ session_uuid, grade                     │ │
│  │ │  ├─ started_at, current_part, remaining_time │ │
│  │ │  └─ security_log (JSON)                     │ │
│  │ │                                               │ │
│  │ ├─ questions (問題)                            │ │
│  │ │  ├─ id, event_id (FK), part, question_number │ │
│  │ │  ├─ text, image_url                         │ │
│  │ │  └─ created_at, updated_at                  │ │
│  │ │                                               │ │
│  │ ├─ choices (選択肢)                            │ │
│  │ │  ├─ id, question_id (FK), label             │ │
│  │ │  ├─ text, image_url, is_correct             │ │
│  │ │  └─ created_at, updated_at                  │ │
│  │ │                                               │ │
│  │ └─ answers (解答記録)                          │ │
│  │    ├─ id, exam_session_id (FK), question_id (FK) │ │
│  │    ├─ user_choice, is_correct                │ │
│  │    └─ answered_at, created_at                │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  【練習問題層 - Practice Layer】                      │
│  ┌──────────────────────────────────────────────────┐ │
│  │ ├─ practice_questions (練習問題)               │ │
│  │ │  ├─ id, section, question, text             │ │
│  │ │  ├─ options (JSON: A/B/C/D)                │ │
│  │ │  ├─ answer, explanation                    │ │
│  │ │  └─ created_at, updated_at                 │ │
│  │ │                                              │ │
│  │ └─ practice_choices (練習問題選択肢)          │ │
│  │    ├─ id, practice_question_id (FK)           │ │
│  │    ├─ label, text, is_correct                │ │
│  │    └─ created_at, updated_at                 │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  【セキュリティ層 - Security Layer】                  │
│  ┌──────────────────────────────────────────────────┐ │
│  │ └─ exam_violations (不正検知ログ)              │ │
│  │    ├─ id, exam_session_id (FK)                │ │
│  │    ├─ violation_type (enum)                  │ │
│  │    ├─ details (JSON)                         │ │
│  │    └─ created_at                             │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 🔗 リレーションシップ

```
Users (1)
  │
  ├─────────── 1:N ───────────────┐
  │                                ▼
  │                          ExamSessions
  │                                ▲
  │                                │ 1:N
  │                                │
  │                        ┌───────┴────────┐
  │                        │                │
  │                    Answers        ExamViolations
  │                        ▲
  │                        │ 1:N
  │                        │
  │                    Questions
  │                        │
  │                        ├── 1:N ────┐
  │                        │           ▼
  │                        │        Choices
  │                        │
  │                    Events (1)
  │                        │
  │                        └─ 1:N ──→ ExamSessions


Admins (1) ──────→ Event管理・成績確認


PracticeQuestions (1)
  │
  └─── 1:N ──→ PracticeChoices
```

---

## 🔒 セキュリティアーキテクチャ

```
┌──────────────────────────────────┐
│ Layer 1: Network Security        │
│ ├─ HTTPS/TLS                     │
│ ├─ Firewall                      │
│ └─ Rate Limiting                 │
└──────────────────────────────────┘
           ▼
┌──────────────────────────────────┐
│ Layer 2: CORS & CSRF             │
│ ├─ CSRF Token                    │
│ ├─ CORS Check                    │
│ └─ SameSite Cookie               │
└──────────────────────────────────┘
           ▼
┌──────────────────────────────────┐
│ Layer 3: Authentication          │
│ ├─ Email Verification            │
│ ├─ Password Hash (bcrypt)        │
│ └─ Login Flow                    │
└──────────────────────────────────┘
           ▼
┌──────────────────────────────────┐
│ Layer 4: Authorization           │
│ ├─ Guard (web/admin)             │
│ ├─ Middleware Check              │
│ └─ Role-based Access             │
└──────────────────────────────────┘
           ▼
┌──────────────────────────────────┐
│ Layer 5: Input Validation        │
│ ├─ Form Validation               │
│ ├─ Type Check                    │
│ └─ Range Check                   │
└──────────────────────────────────┘
           ▼
┌──────────────────────────────────┐
│ Layer 6: Data Protection         │
│ ├─ SQL Injection Prevention       │
│ ├─ XSS Prevention                │
│ ├─ Encryption (passwords)        │
│ └─ Hashing                       │
└──────────────────────────────────┘
           ▼
┌──────────────────────────────────┐
│ Layer 7: Anomaly Detection       │
│ ├─ Violation Logging             │
│ ├─ Unusual Pattern Detection     │
│ └─ Admin Alert                   │
└──────────────────────────────────┘
```

---

## 📊 APIエンドポイント一覧

```
【認証関連】
POST /login              ← ユーザーログイン
POST /register           ← ユーザー登録
GET  /logout             ← ログアウト
POST /admin/login        ← 管理者ログイン

【セッションコード】
POST /session-code/verify ← セッションコード検証
GET  /welcome            ← ウェルカムページ

【試験実施 (ユーザー)】
GET  /test-start         ← 試験開始画面
POST /exam/{id}/start    ← 試験開始
GET  /exam/part/{n}      ← パート表示
POST /exam/answer        ← 解答送信
GET  /result/{id}        ← 結果表示

【試験実施 (ゲスト)】
GET  /guest/info         ← ゲスト情報入力
POST /guest/info/submit  ← 情報送信
GET  /guest/exam/part/{n} ← パート表示
POST /guest/exam/answer  ← 解答送信
GET  /guest/result       ← 結果表示

【練習問題】
GET  /practice           ← 練習問題一覧
GET  /practice/{id}      ← 練習問題詳細

【管理者】
GET  /admin/dashboard    ← ダッシュボード
GET  /admin/events       ← イベント一覧
POST /admin/events       ← イベント作成
GET  /admin/results      ← 成績確認
GET  /admin/violations   ← 不正管理
```

# Draw.io用 - システム全体構成図

## 📐 システム全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                      クライアント層                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────┐    ┌─────────────────┐               │
│  │   一般ユーザー   │    │  ゲストユーザー  │               │
│  │   (認証)        │    │   (セッション)   │               │
│  └────────┬────────┘    └────────┬────────┘               │
│           │                      │                        │
│           └──────────┬───────────┘                        │
│                      │                                    │
│              ┌───────▼────────┐                          │
│              │  Vue 3 + Inertia.js                        │
│              │  Tailwind CSS                              │
│              │  (SPA)                                     │
│              └───────┬────────┘                          │
│                      │                                    │
│              HTTP/HTTPS (CSRF保護)                       │
│                      │                                    │
└──────────────────────┼──────────────────────────────────┘
                       │
        ┌──────────────▼──────────────┐
        │     Laravel 11 Server       │
        │   (PHP 8.4 + Inertia)      │
        │                            │
        │  ┌─ Controllers ──────┐   │
        │  │ - AuthController  │   │
        │  │ - ExamController  │   │
        │  │ - AdminController │   │
        │  └────────────────────┘   │
        │                            │
        │  ┌─ Middleware ──────┐    │
        │  │ - CSRF Token      │    │
        │  │ - Auth Verify     │    │
        │  │ - Admin Auth      │    │
        │  └────────────────────┘    │
        │                            │
        │  ┌─ Models ──────────┐    │
        │  │ - User            │    │
        │  │ - Admin           │    │
        │  │ - ExamSession     │    │
        │  │ - Questions/etc   │    │
        │  └────────────────────┘    │
        │                            │
        └────┬────────────────────────┘
             │
             │ (DB Query)
             │
        ┌────▼─────────────────┐
        │   MySQL Database     │
        │   (8.0)              │
        │                      │
        │ ┌─ users            │
        │ ├─ admins           │
        │ ├─ events           │
        │ ├─ exam_sessions    │
        │ ├─ questions        │
        │ ├─ answers          │
        │ ├─ practice_*       │
        │ └─ ...              │
        │                      │
        └──────────────────────┘
```

## 🔄 リクエスト/レスポンスフロー概要

```
クライアント              サーバー              データベース
    │                       │                      │
    │──── GET / ──────────→ │                      │
    │                       │ (Welcome Page)       │
    │ ←──── HTML + Props ── │                      │
    │                       │                      │
    │──── POST /login ──────→ │                      │
    │                       │                      │
    │                       │──── SELECT * FROM users ──→│
    │                       │                      │     │
    │                       │ ←── User Data ───────│     │
    │                       │ (認証処理・セッション作成)   │
    │ ←── Redirect ────────│                      │
    │                       │                      │
    │──── GET /exam ────────→ │                      │
    │                       │ (セッション確認)        │
    │                       │──── SELECT * FROM exam_sessions ──→│
    │ ←── Exam Page ───────│ ←── Session Data ──│
    │                       │                      │
    │──── POST /answer ────→ │                      │
    │   (JSON: answer_data) │ (解答保存)            │
    │                       │                      │
    │                       │──── INSERT INTO answers ──→│
    │                       │                      │     │
    │                       │ ←── Success ────────│     │
    │ ←── OK + Props ──────│                      │
```

## 🛡️ 認証フロー

```
┌─────────────────┐
│  ログインページ   │
└────────┬────────┘
         │ email + password
         ▼
┌─────────────────────┐
│  AuthController     │
│  - validate input   │
│  - check email      │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐      ┌──────────────────┐
│  User Model         │─────→│  Database Query  │
│  (find by email)    │      │  (users table)   │
└────────┬────────────┘      └──────────────────┘
         │
         ▼ (password hash verify)
┌─────────────────────┐
│  Password Verified? │
└────────┬────────────┘
         │
    ┌────┴────┐
    │YES      │NO
    ▼         ▼
  ✓OK     ✗Error
  │         │
  ▼         ▼
Session   Error Msg
Created   to Client
  │
  ▼
Redirect
to Dashboard
```
